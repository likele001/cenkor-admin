# 开发者中心 · 伪静态（SPA 路由回退）
# 网站 → developer.cenkor.cn → 设置 → 伪静态
set $spa_fallback 0;
if ($uri !~ ^/api/) {
    set $spa_fallback 1;
}
if (!-e $request_filename) {
    set $spa_fallback "${spa_fallback}1";
}
if ($spa_fallback = 11) {
    rewrite ^ /index.html last;
}
